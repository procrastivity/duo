package devin

// LaunchMintPrompt is the print -p / --print text stage1LeafAugmenter appends
// so launch mints a sessions.db row and exits; it is not a skill instruction.
const LaunchMintPrompt = "Reply with exactly: DUO-DEVIN-READY"
